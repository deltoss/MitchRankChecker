{
  "name": "mitchrankchecker",
  "version": "1.0.0",
  "description": "A DotNet core rank checker application. NPM is used to generate docs based on markdown files.",
  "dependencies": {
    "docsify-cli": "^4.3.0"
  },
  "scripts": {
    "init-docs": "docsify init ./docs",
    "serve-docs": "docsify serve ./docs"
  }
}